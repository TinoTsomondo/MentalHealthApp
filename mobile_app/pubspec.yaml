name: mobile_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  fl_chart: ^0.66.2  # Updated to the latest version
  intl: ^0.18.1
  audioplayers: ^5.2.1
  url_launcher: ^6.0.20
  socket_io_client: ^2.0.3+1
  provider: ^6.0.0
  http: ^1.2.1  # Updated to the latest version

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1  # Updated to the latest version

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo.png
    - assets/images/background.jpg